<li>
	<%= link_to story.story_title, story %>
	
	<% if signed_in? && current_user == @user %>
		| <%= link_to "delete", story, :method => :delete, :confirm => "Are you sure?" %>
	<% end %>
	
</li>